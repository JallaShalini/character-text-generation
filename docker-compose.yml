services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: char-text-generation
    working_dir: /app
    volumes:
      - .:/app
    tty: true
    stdin_open: true
